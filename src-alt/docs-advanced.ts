/**
 * This is a complex function that demonstrates code blocks.
 *
 * You can use it like this:
 * ```typescript
 * const res = complexTask('input');
 * console.log(res);
 * ```
 *
 * It handles multiple stages of processing.
 *
 * @param {string} input The input string
 * @returns {string} The processed result
 */
export function complexTask(input: string): string {
  return `Processed: ${input}`;
}

/**
 * This is a placeholder documentation.
 * Just a short one.
 */
export function shortDoc() {
  return null;
}

/**
 * Empty comment for documentation purposes.
 */
export function emptyDoc() {
  return null;
}
